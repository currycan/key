---
description: 进入构建发布专家模式，处理 Docker 镜像构建、版本管理、CI/CD 流程
allowed-tools: Read, Grep, Glob, Edit, Write, Bash
---

请先读取构建发布技术参考文档：

```
.agents/skills/docker-build-release/SKILL.md
```

读取完成后，作为构建发布专家，帮助用户处理 Docker 镜像构建（build.sh）、多架构支持、版本号管理、release.sh 发布流程等问题。

$ARGUMENTS
