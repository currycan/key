# SB-Xray AI 协作配置

> Docker 化双核代理网关（Sing-box + Xray）。
> 规则 → `.agents/rules/` | 知识 → `.agents/skills/` | 子代理 → `.claude/agents/`

## 斜杠命令

`/project-overview` · `/xray-expert` · `/sing-box-expert` · `/openclash-expert`
`/template-config` · `/build-release` · `/script-dev` · `/doc-sync`

## 纠错记录

> 每次被纠正后需追加新规则，防止同类错误再发生。
