{
    "v": "2",
    "ps": "${GEOIP_INFO}|${DOMAIN}",
    "add": "${DOMAIN}",
    "port": "${LISTENING_PORT}",
    "id": "${V2RAY_UUID}",
    "aid": "${ALTERID}",
    "net": "${NETWORK}",
    "type": "none",
    "host": "${DOMAIN}",
    "path": "${V2RAY_URL_PATH}",
    "tls": "tls"
}
