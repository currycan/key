{
    "v": "2",
    "ps": "${GEOIP_INFO}|${DOMAIN}|v2ray",
    "add": "${DOMAIN}",
    "port": "${LISTENING_PORT}",
    "id": "${UUID}",
    "aid": "${ALTERID}",
    "net": "${NETWORK}",
    "type": "none",
    "host": "${DOMAIN}",
    "path": "${URL_PATH}",
    "tls": "tls"
}
