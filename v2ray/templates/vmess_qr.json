{
    "v": "2",
    "ps": "${DOMAIN}",
    "add": "${DOMAIN}",
    "port": "${LISTENING_PORT}",
    "id": "${VMESS_ID}",
    "aid": "${ALTERID}",
    "net": "${NETWORK}",
    "type": "none",
    "host": "${DOMAIN}",
    "path": "${URL_PATH}",
    "tls": "tls"
}
